| События
div(ng-controller="logControl")
  //div(ng-controller="sysselect")
  sysselect(ng-selection-change="on_select", pre-url="/log")
  |  skey:{{skey}}
  a.btn(ng-click="dont_press()") Не нажимать!!!

  table.table.table-striped.logtable
    thead
      tr
        th Дата/время
        th Событие
    tbody
      //tr.tanimation(ng-repeat="l in getlogs(selection)", ui-animate="ui-hide")
      //tr.tanimation(ng-repeat="l in getlogs2(selection).logs", ui-animate="ui-hide")
      tr.tanimation(ng-repeat="l in logs", ui-animate="ui-hide")
        td
          a(ng-click="dellog(skey, l.lkey)"): i(title="Это глюк! Удалить это событие.").icon-trash
          | {{ l.dt }}
        td {{ l.text }}
  a.btn.btn-small(ng-click="more()")
    i.icon-plus
    | Еще...
