"use strict";

(function(window) {
    window.config = {};
    window.config.inits = [];
    window.config.inits.push(function(){
        console.log(['== First init (TBD!). Config:', config]);
    });
})(window);
